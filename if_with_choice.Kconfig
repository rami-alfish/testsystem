menuconfig FEATURE_MENU
    bool "Feature Group"
    default y

if FEATURE_MENU

choice
    prompt "Select your mode"
    tristate

config MODE_FAST
    bool "Fast mode"

config MODE_SAFE
    bool "Safe mode"

endchoice

endif
